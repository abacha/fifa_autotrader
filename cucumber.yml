---
  default: -p headless -p elk_logger
  debug: -p chrome -p elk_logger

  elk_logger: --format ElkLogger::CucumberFormatter
  pretty: --format pretty
  json: --format json --out=log/report.json

  chrome: BROWSER=chrome
  headless: BROWSER=chrome_headless
